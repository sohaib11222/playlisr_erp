<style>
body.pos-v2 .ams-wrap { max-width: 1000px; margin: 0 auto; padding: 18px 16px 60px; font-family: "Inter Tight", system-ui, sans-serif; color: var(--pos-ink); }
body.pos-v2 .ams-wrap h1 { font-size: 24px; font-weight: 700; margin: 0 0 4px; }
body.pos-v2 .ams-wrap .sub { color: #6b6253; margin: 0 0 20px; font-size: 14px; }
body.pos-v2 .ams-card { background: var(--pos-surface); border: 1px solid var(--pos-line); border-radius: 14px; padding: 18px 20px; margin-bottom: 20px; }
body.pos-v2 .ams-card h2 { font-size: 16px; font-weight: 700; margin: 0 0 14px; display: flex; align-items: center; gap: 8px; }
body.pos-v2 .ams-card h2 .fa { color: var(--pos-accent-deep); }
body.pos-v2 .ams-row { display: flex; flex-wrap: wrap; gap: 16px; }
body.pos-v2 .ams-field { display: flex; flex-direction: column; gap: 5px; margin-bottom: 4px; flex: 1 1 200px; min-width: 0; }
body.pos-v2 .ams-field label { font-size: 12px; font-weight: 600; color: #5a5145; }
body.pos-v2 .help-block { font-size: 11.5px; color: #8a8070; margin: 4px 0 0; }
body.pos-v2 .ams-wrap .form-control,
body.pos-v2 .ams-field input,
body.pos-v2 .ams-wrap textarea {
  border: 1px solid var(--pos-line-2); border-radius: 9px; padding: 9px 11px; font-size: 14px;
  font-family: inherit; background: #fff; box-shadow: none; height: auto; min-width: 0; color: var(--pos-ink); }
body.pos-v2 .ams-wrap textarea { line-height: 1.5; }
body.pos-v2 .ams-wrap .form-control:focus,
body.pos-v2 .ams-field input:focus,
body.pos-v2 .ams-wrap textarea:focus {
  outline: none; border-color: var(--pos-accent-deep); box-shadow: 0 0 0 3px var(--pos-accent-soft); }
body.pos-v2 .btn-accent { background: var(--pos-accent); color: var(--pos-accent-text); border: 1px solid var(--pos-accent-deep);
  border-radius: 10px; padding: 10px 22px; font-weight: 700; font-size: 14px; cursor: pointer; font-family: inherit; }
body.pos-v2 .btn-accent:hover { background: var(--pos-accent-deep); }
body.pos-v2 .btn-ghost { background: transparent; border: 1px solid var(--pos-line-2); border-radius: 10px;
  padding: 10px 18px; font-weight: 600; font-size: 14px; cursor: pointer; color: #5a5145; font-family: inherit; text-decoration: none; display: inline-block; }
body.pos-v2 .btn-ghost:hover { background: var(--pos-surface-2); color: #5a5145; }
body.pos-v2 .ams-actions { display: flex; justify-content: flex-end; gap: 10px; }
body.pos-v2 .ams-warnbox { border-color: #e6cd8f; background: #fbf3da; }
body.pos-v2 .ams-warnbox h2 .fa { color: #c8901f; }
body.pos-v2 .ams-openlist { list-style: none; margin: 0; padding: 0; }
body.pos-v2 .ams-openlist li { padding: 8px 0; border-bottom: 1px solid #ecdfb8; font-size: 14px; }
body.pos-v2 .ams-openlist li:last-child { border-bottom: 0; }
body.pos-v2 .ams-openlist .muted { color: #8a7c55; font-weight: 400; }
body.pos-v2 .ams-openlist .snip { font-size: 12.5px; margin-top: 2px; }
body.pos-v2 .ams-statusrow { display: flex; align-items: center; gap: 10px; flex-wrap: wrap; }
body.pos-v2 .ams-statusrow select { border: 1px solid var(--pos-line-2); border-radius: 9px; padding: 9px 11px; font-size: 14px; font-family: inherit; background: #fff; min-width: 220px; color: var(--pos-ink); }
</style>
